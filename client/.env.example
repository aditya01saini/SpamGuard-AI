# Frontend environment (SAFE TO COMMIT — placeholder)
# NOTE: Never put MISTRAL_API_KEY or any backend secret here.
# In development, leave this unset to use the Vite proxy (/api -> localhost:8000).
VITE_API_BASE_URL=/api
